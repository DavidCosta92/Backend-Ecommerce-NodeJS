// @ts-nocheck

// import express, { Router } from 'express';
// import { renderCartsView, renderProductsView, renderCartViewById } from '../controllers/views/views.controller.js';
// import { onlyAuthenticatedWeb , onlyAdminWeb, onlyUserWeb , authenticatorWeb} from '../middlewares/authenticator.js';

//export const viewsRouter = Router();

// viewsRouter.use(express.json()); 

// viewsRouter.get("/products", authenticatorWeb, onlyAuthenticatedWeb, renderProductsView)



// viewsRouter.get("/carts", authenticatorWeb, onlyAuthenticatedWeb, onlyAdminWeb, renderCartsView)
// viewsRouter.get("/carts/:cid", authenticatorWeb, onlyAuthenticatedWeb, onlyAdminWeb, renderCartViewById)

// viewsRouter.get("/chat", authenticatorWeb, onlyAuthenticatedWeb ,/*, onlyUserWeb*/(req, res, next)=>{
//     res.render("chats", {title: "Chat"})
// })
