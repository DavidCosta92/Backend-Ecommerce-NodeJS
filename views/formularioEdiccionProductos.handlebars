
    {{#if loguedUser}}
        <div class=" d-flex flex-wrap border border-success rounded p-4 m-4 justify-content-around">
        {{#if user.username}}
            <h5 class="w-100">{{user.role}}</h5>
            <p class="pe-5 w-25">Username Github: {{user.username}}</p>
            <p class="pe-5 w-50">Rol de usuario : {{user.role}}</p>    
            <p class="pe-5 w-50">ID carrito: <span id="cidUser">{{user.cart}}</span> </p>            
        {{else}}
            <h5 class="w-100">{{user.role}}</h5>
            <p class="pe-5 w-25">Nombre: {{user.first_name}}</p>
            <p class="pe-5 w-25">Apellido: {{user.last_name}}</p> 
            <p class="pe-5 w-25">Email: {{user.email}}</p>
            <p class="pe-5 w-25">Edad: {{user.age}}</p>       
            <p class="pe-5 w-50">Rol de usuario : {{user.role}}</p>    
            <p class="pe-5 w-50">ID carrito: <span id="cidUser">{{user.cart}}</span> </p>  
            <p class="pe-5 w-50">ID usuario: <span id="userId">{{user._id}}</span> </p>  
        {{/if}} 
            <div class="container  w-75 d-flex  align-items-center justify-content-around ">
                <button class="btn btn-outline-dark  m-2 w-50" onClick=goToHome()> Home </button>
                <button class="btn btn-outline-primary   m-2 w-50" onclick="goToUserProfile()">Ver perfil</button>
                <button class="btn btn-outline-success   m-2 w-50" onclick="redirigirProductos()">Ver productos</button>
                <form id="formLogOut" class="m-2 w-50">
                    <button type="submit" class="btn btn-outline-danger w-100">Cerrar sesion</button>
                </form>
                {{#if user.adminOrPremium}}
                <button class="btn btn-outline-info  m-2 w-50" onClick=goToAddProducts()> Crear productos </button>
                {{/if}}           
                {{#if user.admin}}
                <button class="btn btn-outline-warning  m-2 w-50" onClick=goToMembershipsList()> Ver usuarios </button>
                {{/if}}
            </div>
        </div> 
    {{/if}}
<h1 class=" p-3">Form para editar productos </h1>
<div class=" p-3 text-end border border-success rounded p-4 m-4">
    <form class="d-flex flex-wrap" id="formEditProduct" action="/api/products" method="post">
        <div class="mb-3 d-flex w-50">
            <label for="title" class="form-label p-1 w-25">Nombre</label>
            <input type="text" class="form-control 1 w-75" id="title" name="title" aria-describedby="title" value={{product.title}}>
        </div>
        <div class="mb-3 d-flex  w-50">
            <label for="code" class="form-label p-1 w-25">Codigo</label>
            <input type="text" class="form-control w-75" id="code" name="code" aria-describedby="code" value={{product.code}}>
        </div>
        <div class="mb-3 d-flex w-100">
            <label for="description" class="form-label p-1 w-10">Descripcion</label>
            <input type="text" class="form-control w-90" id="description" name="description"  aria-describedby="description" value={{product.description}}>
        </div>

        <div class="mb-3 d-flex w-25">
            <label for="price" class="form-label p-1 w-10">Precio</label>
            <input type="number" class="form-control " id="price" name="price"  aria-describedby="price" min="0" value={{product.price}}>
        </div>
        <div class="mb-3 d-flex w-50">
            <label for="category" class="form-label p-1 w-15">Categoria</label>
            <input type="text" class="form-control " id="category" name="category"  aria-describedby="category" value={{product.category}}>
        </div> 
        <div class="mb-3 d-flex w-25">
            <label for="stock" class="form-label p-1 w-10">Stock</label>
            <input type="number" class="form-control " id="stock" name="stock" aria-describedby="stock" min="1" value={{product.stock}}>
        </div>
        <div class="mb-3 d-flex w-100">
            <label for="thumbnails" class="form-label p-1 ">Thumbnails</label>
            <input type="text" class="form-control" id="thumbnails" name="thumbnails" aria-describedby="thumbnails" value={{product.thumbnails}}>
        </div>     
         <input type="text" class="form-control " id="owner" name="owner" aria-describedby="owner" hidden value={{user.email}}>
        <button type="submit" class="btn btn-primary w-50 mt-3">Enviar</button>
    </form>
</div>

<script src="/js/productsEdit.js"></script>
<script src="/js/userNavigation.js"></script>